import path from 'path';

export const ORG_NAME = 'uRepairPC';

export const ROOT = path.resolve(__dirname, '..');

export const INPUT_DIR = ROOT + path.sep + 'temp';

export const OUTPUT_DIR = ROOT + path.sep + 'build';

export const MOVE_DIR = ROOT + path.sep + 'move';
